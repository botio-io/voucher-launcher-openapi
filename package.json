{
  "name": "voucher-launcher",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.5.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint",
    "version": "redocly --version"
  }
}
